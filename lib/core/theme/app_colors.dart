import 'package:flutter/material.dart';

abstract class AppColors {
  // Dark
  static const darkBackground = Color(0xFF0B0D17);
  static const darkSurface = Color(0xFF1A1D2E);
  static const darkSurfaceLight = Color(0xFF252840);
  static const darkDivider = Color(0xFF2A2D3E);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFB0B3C5);

  // Light
  static const lightBackground = Color(0xFFF5F5FA);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceLight = Color(0xFFEEEEF5);
  static const lightDivider = Color(0xFFE0E0E8);
  static const lightTextPrimary = Color(0xFF1A1D2E);
  static const lightTextSecondary = Color(0xFF6B6F80);

  // Shared
  static const primary = Color(0xFF6C63FF);
  static const secondary = Color(0xFF00D4AA);
  static const accent = Color(0xFFFF6B6B);
}
